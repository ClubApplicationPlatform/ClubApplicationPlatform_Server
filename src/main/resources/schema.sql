CREATE TABLE CLUB (
                      id BIGINT AUTO_INCREMENT PRIMARY KEY,
                      name VARCHAR(255),
                      member_count INT,
                      recruiting BOOLEAN
);
